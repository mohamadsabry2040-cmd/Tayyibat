import 'package:flutter/material.dart';

class BirdsView extends StatelessWidget {
  const BirdsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('أماكن بيع الطيور والأرانب'),
      ),
    );
  }
}